#include "pch.h"
#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
#include <utility>
#include <set>
#include <string>
using namespace std;

#define vi vector<int>
#define pb push_back

int main()
{
	int N, a;
	string word;
	int K;
	char c;
	cin >> N;
	cin >> word;
	cin >> K;
	vi amount(K);
	for (int i = 0; i < K; i++) {
		cin >> a;
		cin >> c;

	}
}